{
  "name": "contenstack-gatsby-integrations",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build:gsp": "npm run build -w gatsby-source-contentstack",
    "watch:gsp": "npm run watch -w gatsby-source-contentstack",
    "develop:starter-app": "npm run develop -w contentstack-gatsby-starter-app"
  },
  "private": true,
  "dependencies": {},
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
